# Simple test for the syntax of big types

# Big natural numbers
data Nat : TYPE where
| zero : Nat
| suc (n : Nat) : Nat
end

data Pair (A : Type) (B (a : A) : Type) :: TYPE where
| pair (a : A) (b : B a) : Pair A ((x : A) -> B x)
end
